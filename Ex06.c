#include<stdio.h> 
#include<string.h> 
#include<ctype.h> 
#include<locale.h> 
#include<stdlib.h>
int main(void)
{
	int matricula[100];
	int salario[100];
	int i = 0;
	char adicionar;

	do {
		printf("Matricula: ");
		scanf("%d", &matricula);


	} while (adicionar == 'S' || adicionar == 's');

  return 0;
}
